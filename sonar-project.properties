sonar.projectKey=JoshnaChary_notes_application
sonar.organization=joshnachary
sonar.projectName=notes_application

# Source and test directories
sonar.sources=lib
sonar.tests=test

# THIS IS THE MOST IMPORTANT LINE
# Coverage report path — must match exactly
# where flutter test --coverage generates it
sonar.dart.lcov.reportPaths=coverage/lcov.info

# Exclude auto generated and platform code
sonar.exclusions=**/generated/**,\
                 **/*.g.dart,\
                 **/*.freezed.dart,\
                 **/build/**,\
                 windows/runner/**,\
                 linux/runner/**,\
                 macos/Runner/**,\
                 ios/Runner/**,\
                 android/app/src/main/res/**

# Exclude test files from coverage
sonar.coverage.exclusions=**/*_test.dart,\
                           **/generated/**,\
                           **/*.g.dart,\
                           windows/runner/**,\
                           linux/runner/**

sonar.sourceEncoding=UTF-8

